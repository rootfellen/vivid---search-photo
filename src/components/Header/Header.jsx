import React from "react";
import { Logo, Container } from "./HeaderElements";

const Header = () => {
  return (
    <Container>
      <Logo>vivid.</Logo>
    </Container>
  );
};

export default Header;
